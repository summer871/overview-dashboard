name: Index superseded CSS cleanup

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/ai-readable-cleanup-2026-08-21
      - run: test -s docs/INDEX-SUPERSEDED-CSS-CLEANUP-2026-08-21.json
