{
  "name": "overview-dashboard",
  "private": true,
  "version": "0.1.0",
  "description": "Google Apps Script dashboard source and validation tooling.",
  "scripts": {
    "audit:components": "node scripts/audit-dashboard-components.js",
    "audit:dashboard": "npm run audit:components"
  },
  "engines": {
    "node": ">=18"
  }
}
