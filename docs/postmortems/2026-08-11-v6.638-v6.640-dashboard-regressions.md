# Incident Postmortem — 2026-08-11 — v6.638/v6.640 Dashboard Regressions

Technical record for the v6.638/v6.640 regressions and v6.641 recovery.

- Known-good UI baseline: `03a991f0fa2de15905083ef2f0f11abc44bf086e`
- Failed v6.640 commit: `c731b3bde8d02c339379fc4d5d0ba02f466eb629`
- ClickUp postmortem: https://app.clickup.com/9017485301/docs/8cqqtzn-2937/8cqqtzn-6177

## Guardrails
1. Do not modify `SharedDashboardLayoutEditorV6593.html` for a localized defect without architecture-level justification and explicit real-browser regression coverage.
2. Synthetic geometry/unit tests are not browser QA.
3. Existing persisted browser layout must be included in real `/dev` acceptance testing.
4. Handoff/server/runtime disagreement is a stop condition; diagnose read-only first.
5. Restore the last known-good baseline before continuing feature work after a visual regression.
6. Do not bundle attribution/data-path changes with shared layout-engine changes.
7. Do not reset saved layout during routine diagnosis unless separately authorized.
8. Recovery checks only intended files; unrelated untracked files are preserved.
9. Do not paste large recovery programs interactively into PowerShell; use a saved `.ps1` or small inspectable commands.
10. Do not rerun the root-attribution backfill or rebuild the browser-ready cache unless separately authorized.
11. Do not infer success from validators when real `/dev` fails.
12. Treat GitHub, Apps Script head, production, browser cache, and persisted browser layout as separate states.

## Attribution evidence
The existing browser-ready gzip is healthy: 119,589 rows, 6,521 remake rows with attribution indexes, 6,073 attribution dictionary records, 4,215 exact-root rows, 2,306 review/unresolved rows, and zero unsafe fallback rows. Case 375669 correctly resolves to root case 361499 with `REVIEW_AMBIGUOUS_ROOT_PRODUCT`.

The loaded browser state still reports integration inactive, state enabled false, and state metadata null. The remaining attribution defect is therefore client-side after the healthy browser-ready payload is produced.

## v6.641 recovery target
Restore `Index.html` and `SharedDashboardLayoutEditorV6593.html` to the v6.639 baseline, preserve the 10.5 px footer and bump visible UI identity to v6.641, and make no attribution/cache/backfill/property/trigger/production changes.
