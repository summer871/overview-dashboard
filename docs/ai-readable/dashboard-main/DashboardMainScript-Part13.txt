      #remakeFactorPage.cleanRemakeV6230 .ceramistFlagButtonV6342 { height:25px !important; border:1px solid #cfd6df !important; background:#fff !important; color:#475467 !important; border-radius:999px !important; padding:0 9px !important; font:900 10.5px/1 Roboto,Arial,sans-serif !important; cursor:pointer !important; white-space:nowrap !important; }
      #remakeFactorPage.cleanRemakeV6230 .ceramistFlagButtonV6342.active { background:#fff7d6 !important; color:#7a4a00 !important; border-color:#f2c94c !important; box-shadow:0 1px 2px rgba(245,158,11,.18) !important; }
      #remakeFactorPage.cleanRemakeV6230 .ceramistPreviewStatusV6342 { color:#667085 !important; font:800 10px/1.35 Roboto,Arial,sans-serif !important; text-align:left !important; white-space:normal !important; }
      #remakeFactorPage.cleanRemakeV6230 .ceramistPreviewStatusV6342:empty { display:none !important; }
      #remakeFactorPage.cleanRemakeV6230 .ceramistPreviewKpisV6342 { display:flex !important; align-items:stretch !important; justify-content:flex-start !important; flex-wrap:wrap !important; gap:6px !important; padding:7px 10px !important; border-bottom:1px solid #edf0f5 !important; }
      #remakeFactorPage.cleanRemakeV6230 .ceramistPreviewKpiV6342 { flex:0 1 165px !important; min-width:125px !important; max-width:180px !important; padding:6px 8px !important; border:1px solid #e4e8ef !important; border-radius:7px !important; background:#f8fafc !important; }
      #remakeFactorPage.cleanRemakeV6230 .ceramistPreviewKpiV6342 .label { color:#667085 !important; font:900 8.75px/1 Roboto,Arial,sans-serif !important; letter-spacing:.04em !important; text-transform:uppercase !important; margin-bottom:3px !important; }
      #remakeFactorPage.cleanRemakeV6230 .ceramistPreviewKpiV6342 .value { color:#172033 !important; font:900 15px/1.05 Roboto,Arial,sans-serif !important; white-space:nowrap !important; }
      #remakeFactorPage.cleanRemakeV6230 .ceramistWorkspaceV6343 { display:grid !important; grid-template-columns:minmax(360px,.78fr) minmax(560px,1.22fr) !important; min-height:330px !important; }
      #remakeFactorPage.cleanRemakeV6230 .ceramistPanelV6343 { min-width:0 !important; overflow:hidden !important; }
      #remakeFactorPage.cleanRemakeV6230 .ceramistPanelV6343 + .ceramistPanelV6343 { border-left:1px solid #dfe4eb !important; }
      #remakeFactorPage.cleanRemakeV6230 .ceramistPanelHeaderV6343 { height:39px !important; padding:6px 9px !important; display:flex !important; align-items:center !important; justify-content:space-between !important; gap:8px !important; border-bottom:1px solid #e5e7eb !important; background:#fff !important; }
      #remakeFactorPage.cleanRemakeV6230 .ceramistPanelTitleV6343 { min-width:0 !important; overflow:hidden !important; text-overflow:ellipsis !important; white-space:nowrap !important; color:#202124 !important; font:900 11.5px/1 Roboto,Arial,sans-serif !important; }
      #remakeFactorPage.cleanRemakeV6230 .ceramistPanelHintV6343 { color:#667085 !important; font:800 9.5px/1 Roboto,Arial,sans-serif !important; white-space:nowrap !important; }
      #remakeFactorPage.cleanRemakeV6230 .ceramistBreakdownBarV6343 { display:inline-flex !important; flex:0 0 auto !important; align-items:center !important; gap:2px !important; padding:2px !important; border:1px solid #d8dde6 !important; border-radius:999px !important; background:#eef1f5 !important; }
      #remakeFactorPage.cleanRemakeV6230 .ceramistBreakdownBarV6343 button { border:0 !important; border-radius:999px !important; background:transparent !important; color:#475467 !important; padding:5px 8px !important; font:900 10px/1 Roboto,Arial,sans-serif !important; cursor:pointer !important; white-space:nowrap !important; }
      #remakeFactorPage.cleanRemakeV6230 .ceramistBreakdownBarV6343 button.active { background:#fff7d6 !important; color:#7a4a00 !important; border:1px solid #f2c94c !important; }
      #remakeFactorPage.cleanRemakeV6230 .ceramistPreviewTableWrapV6342 { height:290px !important; }
      #remakeFactorPage.cleanRemakeV6230 .ceramistPreviewTableWrapV6342 .remakeTable { table-layout:fixed !important; }
      #remakeFactorPage.cleanRemakeV6230 #remakeCeramistTableV6342 th:first-child,
      #remakeFactorPage.cleanRemakeV6230 #remakeCeramistTableV6342 td:first-child { text-align:left !important; width:32% !important; }
      #remakeFactorPage.cleanRemakeV6230 #remakeCeramistTableV6342 th:not(:first-child),
      #remakeFactorPage.cleanRemakeV6230 #remakeCeramistTableV6342 td:not(:first-child),
      #remakeFactorPage.cleanRemakeV6230 #remakeCeramistDetailTableV6343 th:nth-child(n+3),
      #remakeFactorPage.cleanRemakeV6230 #remakeCeramistDetailTableV6343 td:nth-child(n+3) { text-align:right !important; }
      #remakeFactorPage.cleanRemakeV6230 #remakeCeramistDetailTableV6343 th:first-child,
      #remakeFactorPage.cleanRemakeV6230 #remakeCeramistDetailTableV6343 td:first-child { text-align:left !important; width:34% !important; }
      #remakeFactorPage.cleanRemakeV6230 #remakeCeramistDetailTableV6343 th:nth-child(2),
      #remakeFactorPage.cleanRemakeV6230 #remakeCeramistDetailTableV6343 td:nth-child(2) { text-align:left !important; width:22% !important; }
      #remakeFactorPage.cleanRemakeV6230 .ceramistUnattributedV6342 { color:#b42318 !important; font-weight:900 !important; }
      #remakeFactorPage.cleanRemakeV6230 .ceramistSelectedV6343 td { background:#fff7d6 !important; border-color:#f2c94c !important; font-weight:900 !important; }
      #remakeFactorPage.cleanRemakeV6230 .ceramistNoteBadgeV6343 { display:inline-flex !important; align-items:center !important; margin-left:5px !important; padding:1px 5px !important; border:1px solid #f3c86a !important; border-radius:999px !important; background:#fff8df !important; color:#8a5300 !important; font:900 8.5px/1.2 Roboto,Arial,sans-serif !important; vertical-align:middle !important; }
      @keyframes fadeOutV6295 { to { opacity:.42; transform:scale(.992); filter:blur(.7px); } }
      @keyframes fadeInV6295 { from { opacity:.35; transform:scale(1.008); filter:blur(1px); } to { opacity:1; transform:scale(1); filter:blur(0); } }

      @media(max-width:980px){#remakeFactorPage.cleanRemakeV6230 .remakeStickyTopV6245{top:0!important;margin:-6px 0 1px!important;}#remakeFactorPage.cleanRemakeV6230 .remakeDropdownPanelV6245{position:fixed!important;left:10px!important;right:10px!important;top:112px!important;min-width:0!important;max-height:calc(100vh - 130px)!important;}}
      @media(max-width:1380px){#remakeFactorPage.cleanRemakeV6230 .remakeKpisV6230{grid-template-columns:repeat(4,minmax(0,1fr)) !important;}#remakeFactorPage.cleanRemakeV6230 .remakeKpiControlsV6279{grid-column:1 / -1 !important; align-items:flex-end !important;}#remakeFactorPage.cleanRemakeV6230 .remakeFilterBarV6230{grid-template-columns:repeat(3,minmax(0,1fr)) !important;}#remakeFactorPage.cleanRemakeV6230 .remakeCustomerSplitV6230{grid-template-columns:1fr !important;}}
      @media(max-width:980px){#remakeFactorPage.cleanRemakeV6230 .remakeGridV6230{grid-template-columns:1fr !important;}#remakeFactorPage.cleanRemakeV6230 .remakeKpisV6230,#remakeFactorPage.cleanRemakeV6230 .remakeFilterBarV6230,#remakeFactorPage.cleanRemakeV6230 .ceramistPreviewKpisV6342{grid-template-columns:repeat(2,minmax(0,1fr)) !important;}#remakeFactorPage.cleanRemakeV6230 .ceramistPreviewMetaV6342{align-items:flex-start!important;flex-direction:column!important;}#remakeFactorPage.cleanRemakeV6230 .ceramistPreviewStatusV6342{text-align:left!important;max-width:none!important;}#remakeFactorPage.cleanRemakeV6230 .ceramistWorkspaceV6343{grid-template-columns:1fr!important;}#remakeFactorPage.cleanRemakeV6230 .ceramistPanelV6343 + .ceramistPanelV6343{border-left:0!important;border-top:1px solid #dfe4eb!important;}#remakeFactorPage.cleanRemakeV6230 .remakeKpiControlsV6279{grid-column:1 / -1 !important; align-items:flex-start !important; text-align:left !important;}#remakeFactorPage.cleanRemakeV6230 .remakeKpiControlsV6279 .remakePerfV6230,#remakeFactorPage.cleanRemakeV6230 .remakeKpiControlsV6279 .remakeRefreshStamp{text-align:left !important;}}
    `;
    document.head.appendChild(style);
  }

  function cardActionsV6230(component) {
    const componentLabel = component === 'trend' ? 'chart' : 'table';
    return '<div class="remakeCardActionsV6230">'
      + '<button class="remakeMiniButtonV6230" type="button" title="Export the totals and rows currently shown in this panel" onclick="exportRemakeComponentV6230(\'' + escJsV6230(component) + '\')">Export current</button>'
      + '<button class="remakeMiniButtonV6230" type="button" title="Export the detailed records behind the current panel and filters" onclick="exportRemakeAllDataV6399(\'' + escJsV6230(component) + '\')">All data</button>'
      + '<button class="remakeMiniButtonV6230" type="button" title="Open a live interactive pop-out" onclick="popoutRemakeComponentV6230(\'' + escJsV6230(component) + '\')">Pop out</button>'
      + '<button class="remakeMiniButtonV6230 backButtonV6266 clearComponentButtonV6304" type="button" title="Clear only this ' + componentLabel + '\'s cross-filter" aria-label="Clear only this ' + componentLabel + '\'s cross-filter" onclick="clearRemakeComponentV6230(\'' + escJsV6230(component) + '\')">&#8634;</button>'
      + '</div>';
  }


  const filterMetaV6245 = {
    year: { inputId: 'remakeYearFilter', buttonId: 'remakeYearButtonV6245', panelId: 'remakeYearPanelV6245', searchId: 'remakeYearSearchV6245', listId: 'remakeYearListV6245', countId: 'remakeYearCountV6245', allLabel: 'All years', label: 'Year' },
    department: { inputId: 'remakeDepartmentFilter', buttonId: 'remakeDepartmentButtonV6245', panelId: 'remakeDepartmentPanelV6245', searchId: 'remakeDepartmentSearchV6245', listId: 'remakeDepartmentListV6245', countId: 'remakeDepartmentCountV6245', allLabel: 'All departments', label: 'Department' },
    product: { inputId: 'remakeProductFilter', buttonId: 'remakeProductButtonV6245', panelId: 'remakeProductPanelV6245', searchId: 'remakeProductSearchV6245', listId: 'remakeProductListV6245', countId: 'remakeProductCountV6245', allLabel: 'All products', label: 'Product' },
    productGroup: { inputId: 'remakeProductGroupFilter', buttonId: 'remakeProductGroupButtonV6245', panelId: 'remakeProductGroupPanelV6245', searchId: 'remakeProductGroupSearchV6245', listId: 'remakeProductGroupListV6245', countId: 'remakeProductGroupCountV6245', allLabel: 'All groups', label: 'Group' },
    customer: { inputId: 'remakeCustomerFilter', buttonId: 'remakeCustomerButtonV6245', panelId: 'remakeCustomerPanelV6245', searchId: 'remakeCustomerSearchV6245', listId: 'remakeCustomerListV6245', countId: 'remakeCustomerCountV6245', allLabel: 'All customers', label: 'Customer' },
    reason: { inputId: 'remakeReasonFilter', buttonId: 'remakeReasonButtonV6245', panelId: 'remakeReasonPanelV6245', searchId: 'remakeReasonSearchV6245', listId: 'remakeReasonListV6245', countId: 'remakeReasonCountV6245', allLabel: 'All reasons', label: 'Reason' }
  };
  // v6.308: [] continues to mean All. This sentinel is the explicit zero-selection state
  // used by the new None button without changing the established filter data model.
  const REMAKE_NONE_FILTER_VALUE_V6308 = '__CDA_REMAKE_NONE_V6308__';
  function filterValuesV6245(kind) { const raw = uiV6230.filters ? uiV6230.filters[kind] : ''; if (Array.isArray(raw)) return raw.map(String).filter(Boolean); if (!raw) return []; try { const parsed = JSON.parse(String(raw)); if (Array.isArray(parsed)) return parsed.map(String).filter(Boolean); } catch(e) {} return [String(raw)]; }
  function excludedFilterValuesV6389(kind) { const raw = uiV6230.filterExclusionsV6389 ? uiV6230.filterExclusionsV6389[kind] : []; if (Array.isArray(raw)) return raw.map(String).filter(Boolean); if (!raw) return []; return [String(raw)]; }
  function filterIsNoneV6308(kind) { const values = filterValuesV6245(kind); return values.length === 1 && values[0] === REMAKE_NONE_FILTER_VALUE_V6308; }
  function setFilterValuesV6245(kind, values) { const seen = new Set(); const clean = []; (Array.isArray(values) ? values : [values]).forEach(value => { const text = String(value === null || value === undefined ? '' : value).trim(); if (text && !seen.has(text)) { seen.add(text); clean.push(text); } }); uiV6230.filters[kind] = clean; if (!uiV6230.filterExclusionsV6389) uiV6230.filterExclusionsV6389 = {}; uiV6230.filterExclusionsV6389[kind] = []; const meta = filterMetaV6245[kind]; const input = meta ? idV6230(meta.inputId) : null; if (input) input.value = JSON.stringify(clean); return clean; }
  function setFilterExclusionsV6389(kind, values) { const seen = new Set(); const clean = []; (Array.isArray(values) ? values : [values]).forEach(value => { const text = String(value === null || value === undefined ? '' : value).trim(); if (text && !seen.has(text)) { seen.add(text); clean.push(text); } }); if (!uiV6230.filterExclusionsV6389) uiV6230.filterExclusionsV6389 = {}; uiV6230.filterExclusionsV6389[kind] = clean; uiV6230.filters[kind] = []; const meta = filterMetaV6245[kind]; const input = meta ? idV6230(meta.inputId) : null; if (input) input.value = '[]'; return clean; }
  function matchesFilterV6245(kind, value) { const selected = filterValuesV6245(kind); if (filterIsNoneV6308(kind)) return false; if (selected.length) return selected.some(item => String(item) === String(value)); const excluded = new Set(excludedFilterValuesV6389(kind)); return !excluded.has(String(value)); }
  function filterOptionLabelV6245(kind, value) { if (String(value) === REMAKE_NONE_FILTER_VALUE_V6308) return 'None'; const options = (uiV6230.filterOptionsV6245 && uiV6230.filterOptionsV6245[kind]) || []; const match = options.find(opt => String(opt.value) === String(value)); return match ? match.label : ''; }
  function multiFilterMarkupV6245(kind, label, allLabel) {
    const meta = filterMetaV6245[kind];
    const visibleToggleId = 'remakeVisibleToggleV6502_' + kind;
    return '<div class="remakeFilter remakeMultiFilterV6245" data-filter-kind="' + escV6230(kind) + '">'
      + '<label for="' + escV6230(meta.buttonId) + '">' + escV6230(label) + '</label>'
      + '<input id="' + escV6230(meta.inputId) + '" class="remakeHiddenSelectV6245" data-multi-managed-v6245="1" type="hidden" value="[]">'
      + '<button id="' + escV6230(meta.buttonId) + '" class="remakeDropdownButtonV6245" type="button" onclick="toggleRemakeDropdownV6245(\'' + escJsV6230(kind) + '\')">' + escV6230(allLabel) + '</button>'
      + '<div id="' + escV6230(meta.panelId) + '" class="remakeDropdownPanelV6245">'
      + '<div class="remakeDropdownHeaderV6245">'
      + '<label class="remakeVisibleToggleV6502" title="Check to select every visible option. Uncheck to deselect every visible option.">'
      + '<input id="' + escV6230(visibleToggleId) + '" type="checkbox" onchange="toggleVisibleRemakeOptionsV6502(\'' + escJsV6230(kind) + '\',this.checked,event)">'
      + '<span>Visible</span></label>'
      + '<button type="button" title="Select all options and remove this filter" aria-label="Reset ' + escV6230(label) + ' filter" onclick="clearRemakeMultiFilterV6245(\'' + escJsV6230(kind) + '\')">Reset</button>'
      + '<button type="button" title="Deselect every option" aria-label="Select no ' + escV6230(label.toLowerCase()) + ' options" onclick="selectNoneRemakeOptionsV6308(\'' + escJsV6230(kind) + '\')">None</button>'
      + '<span id="' + escV6230(meta.countId) + '" class="remakeDropdownCountV6245">0</span></div>'
      + '<input id="' + escV6230(meta.searchId) + '" class="remakeDropdownSearchV6245" type="text" placeholder="Search ' + escV6230(label.toLowerCase()) + '" oninput="renderRemakeDropdownListV6245(\'' + escJsV6230(kind) + '\')">'
      + '<div id="' + escV6230(meta.listId) + '" class="remakeDropdownListV6245"></div></div></div>';
  }

  function buildShellV6230(force) {
    const page = idV6230('remakeFactorPage');
    if (!page) return;
    const wasActive = page.classList.contains('active');
    if (!force && page.getAttribute('data-clean-remake-version') === 'v6230' && page.querySelector('#remakeTrendChartV6230')) return;
    const dockedFilterBarV6337 = document.querySelector('#remakeTabFilterHostV6337 .remakeFilterBarV6230');
    if (dockedFilterBarV6337) dockedFilterBarV6337.remove();
    const dockedCacheActionsV6338 = document.querySelector('#remakeTabFilterHostV6337 .remakeCacheActionBarV6338');
    if (dockedCacheActionsV6338) dockedCacheActionsV6338.remove();
    page.className = 'tabPage cleanRemakeV6230' + (wasActive ? ' active' : '');
    page.setAttribute('data-clean-remake-version', 'v6230');
    page.innerHTML = `
      <div class="remakeShellV6230">
        <div class="remakeStickyTopV6245">
          <div class="remakeHeaderV6230" style="display:none !important;"></div>
          <div id="remakeNotice" class="remakeNotice"></div>
          <div class="remakeGlobalSearchBarV6304">
            <button id="remakeGlobalSearchButtonV6304" class="remakeGlobalSearchButtonV6304" type="button" title="Search every dashboard filter" aria-label="Search every dashboard filter" aria-expanded="false" onclick="toggleRemakeGlobalSearchV6304(event)">
              <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false"><path d="M10.7 4a6.7 6.7 0 1 0 4.13 11.98l3.6 3.6 1.41-1.42-3.59-3.59A6.7 6.7 0 0 0 10.7 4Zm0 2a4.7 4.7 0 1 1 0 9.4 4.7 4.7 0 0 1 0-9.4Z"/></svg>
            </button>
            <div id="remakeGlobalSearchPanelV6304" class="remakeGlobalSearchPanelV6304" role="dialog" aria-label="Search all filters">
              <div class="remakeGlobalSearchInputRowV6304">
                <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false"><path d="M10.7 4a6.7 6.7 0 1 0 4.13 11.98l3.6 3.6 1.41-1.42-3.59-3.59A6.7 6.7 0 0 0 10.7 4Zm0 2a4.7 4.7 0 1 1 0 9.4 4.7 4.7 0 0 1 0-9.4Z"/></svg>
                <input id="remakeGlobalSearchInputV6304" type="search" autocomplete="off" placeholder="Search and select multiple filters" oninput="renderRemakeGlobalSearchV6304()" onkeydown="handleRemakeGlobalSearchKeyV6304(event)">
                <button type="button" class="remakeGlobalSearchCloseV6304" title="Close search" aria-label="Close search" onclick="closeRemakeGlobalSearchV6304()">×</button>
              </div>
              <div id="remakeGlobalSearchResultsV6304" class="remakeGlobalSearchResultsV6304"></div>
              <div id="remakeGlobalSearchActionsV6305" class="remakeGlobalSearchActionsV6305">
                <span id="remakeGlobalSearchCountV6305" class="remakeGlobalSearchCountV6305">0 selected</span>
                <button type="button" class="remakeGlobalSearchClearV6305" title="Remove all five global filters and return them to All" onclick="clearRemakeGlobalSearchDraftV6305()">Reset all filters</button>
                <button type="button" class="remakeGlobalSearchApplyV6305" onclick="applyRemakeGlobalSearchSelectionsV6305()">Apply</button>
              </div>
            </div>
          </div>
          <div id="remakeFilterHomeV6337" aria-hidden="true"></div>
          <div class="remakeFilterBarV6230">
            ${multiFilterMarkupV6245('year', 'Year', 'All years')}
            ${multiFilterMarkupV6245('department', 'Department', 'All departments')}
            ${multiFilterMarkupV6245('product', 'Product', 'All products')}
            ${multiFilterMarkupV6245('productGroup', 'Group', 'All groups')}
            ${multiFilterMarkupV6245('customer', 'Customer', 'All customers')}
            ${multiFilterMarkupV6245('reason', 'Reason', 'All reasons')}
          </div>
          <div class="remakeKpisV6230">
            <div class="remakeKpi" title="Primary KPI shows the current comparison year. The small line compares against the prior year using the same months."><div class="label">Total Cases</div><div id="remakeTotalCases" class="value">-</div><div id="remakeTotalCasesSub" class="sub"></div></div>
            <div class="remakeKpi"><div class="label">Remake Cases</div><div id="remakeCases" class="value">-</div><div id="remakeCasesSub" class="sub"></div></div>
            <div class="remakeKpi"><div class="label">Case Rate</div><div id="remakeCaseRate" class="value">-</div><div id="remakeCaseRateSub" class="sub"></div></div>
            <div class="remakeKpi"><div class="label">Total Units</div><div id="remakeTotalUnits" class="value">-</div><div id="remakeTotalUnitsSub" class="sub"></div></div>
            <div class="remakeKpi"><div class="label">Remake Units</div><div id="remakeUnits" class="value">-</div><div id="remakeUnitsSub" class="sub"></div></div>
            <div class="remakeKpi"><div class="label">Unit Rate</div><div id="remakeUnitRate" class="value">-</div><div id="remakeUnitRateSub" class="sub"></div></div>
            <div class="remakeKpi"><div class="label">Remake Discount</div><div id="remakeDiscount" class="value">-</div><div id="remakeDiscountSub" class="sub"></div></div>
          </div>
          <div id="remakePillsV6230" class="remakePillsV6230" data-placement="below-remake-kpis"></div>
        </div>
        <div class="remakeGridV6230">
          <div class="remakeCard" id="remakeTrendCardV6230">
            <div class="remakeCardHeader"><div class="remakeCardTitle">Monthly Remake Comparison</div>${cardActionsV6230('trend')}</div>
            <div class="remakeChartModeBarV6230">
              <button id="remakeModeLineV6230" type="button" onclick="setRemakeChartModeV6230('line')">Line</button>
              <button id="remakeModeBarV6230" type="button" onclick="setRemakeChartModeV6230('bar')">Bar</button>
              <button id="remakeMeasureCasesV6245" class="measureToggleV6245" type="button" onclick="setRemakeTrendMeasureV6245('cases')">Cases</button>
              <button id="remakeMeasureUnitsV6245" type="button" onclick="setRemakeTrendMeasureV6245('units')">Units</button>
              <button id="remakeValueTotalV6247" class="valueToggleV6247" type="button" onclick="setRemakeTrendValueModeV6247('total')">Total</button>
              <button id="remakeValuePercentV6247" type="button" onclick="setRemakeTrendValueModeV6247('percent')">%</button>
              <div id="remakeTrendViewGroupV6304" class="remakeTrendViewGroupV6304" aria-label="Monthly or cumulative line view" hidden>
                <span class="remakeTrendViewLabelV6304">View</span>
                <button id="remakeMonthlyV6304" class="cumulativeToggleV6302" type="button" title="Show each month separately" onclick="setRemakeTrendCumulativeV6304(false)">Monthly</button>
                <button id="remakeCumulativeV6302" class="cumulativeToggleV6302" type="button" title="Show a running year-to-date total" onclick="setRemakeTrendCumulativeV6304(true)">Cumulative</button>
              </div>
            </div>
            <div class="remakeChartBoxV6230"><canvas id="remakeTrendChartV6230"></canvas></div>
          </div>
          <div class="remakeCard" id="remakeDepartmentCardV6230">
            <div class="remakeCardHeader"><div class="remakeCardTitle">Departments</div><div class="remakeProductHeaderControlsV6264"><button id="remakeDepartmentCompareBtnV6297" class="tableCompareToggleV6297" type="button" onclick="toggleRemakeTableCompareV6297('department')">2025</button>${cardActionsV6230('department')}</div></div>
            <div class="remakeTableWrap"><table id="remakeDepartmentTable" class="remakeTable"></table></div>
          </div>
        </div>
        <div class="remakeGridV6230">
          <div class="remakeCard" id="remakeReasonCardV6230">
            <div class="remakeCardHeader"><div class="remakeCardTitle">Remake Reasons</div><div class="remakeProductHeaderControlsV6264"><button id="remakeReasonCompareBtnV6297" class="tableCompareToggleV6297" type="button" onclick="toggleRemakeTableCompareV6297('reason')">2025</button>${cardActionsV6230('reason')}</div></div>
            <div class="remakeTableWrap"><table id="remakeReasonTable" class="remakeTable"></table></div>
          </div>
          <div class="remakeCard" id="remakeProductCardV6230">
            <div class="remakeCardHeader"><div id="remakeProductTitleV6264" class="remakeCardTitle">Products</div><div class="remakeProductHeaderControlsV6264"><div class="remakeProductModeBarV6264"><button id="remakeProductModeProductBtnV6264" type="button" onclick="setRemakeProductTableModeV6264('product')">Products</button><button id="remakeProductModeGroupBtnV6264" type="button" onclick="setRemakeProductTableModeV6264('group')">Groups</button></div><button id="remakeProductCompareBtnV6297" class="tableCompareToggleV6297" type="button" onclick="toggleRemakeTableCompareV6297('product')">2025</button>${cardActionsV6230('product')}</div></div>
            <div class="remakeTableWrap"><table id="remakeProductTable" class="remakeTable"></table></div>
          </div>
        </div>
        <div class="remakeGridV6230">
          <div class="remakeCard remakeCardWideV6230" id="remakeCustomerCardV6230">
            <div class="remakeCardHeader"><div id="remakeCustomerTitleV6251" class="remakeCardTitle">Customers</div><div class="remakeCustomerHeaderControlsV6251"><button id="remakeCustomerCompareBtnV6297" class="tableCompareToggleV6297" type="button" onclick="toggleRemakeTableCompareV6297('customer')">2025</button>${cardActionsV6230('customer')}</div></div>
            <div class="remakeCustomerSplitV6230">
              <div class="remakeCustomerDetailPanelV6503">
                <div id="remakeCustomerResponsibilitySummaryV6502" class="remakeCustomerResponsibilitySummaryV6502"></div>
                <div class="remakeCustomerChartBoxV6230"><canvas id="remakeCustomerChartV6230"></canvas><div id="remakeCustomerDetailEmptyV6503" class="remakeCustomerDetailEmptyChartV6503">Select a customer, department, product, group, reason, or technician to view its responsibility share.</div></div>
              </div>
              <div class="remakeTableWrap remakeCustomerTableWrapV6230"><table id="remakeCustomerTable" class="remakeTable"></table></div>
            </div>
          </div>
        </div>
        <div class="remakeGridV6230">
          <div class="remakeCard remakeCardWideV6230" id="remakeCeramistCardV6342">
            <div class="remakeCardHeader"><div class="remakeCardTitle">Technicians</div>${cardActionsV6230('ceramist')}</div>
            <div class="ceramistTopControlsV6351">
              <div class="ceramistPreviewKpisV6342">
                <div class="ceramistPreviewKpiV6342"><div class="label">Cases</div><div id="remakeCeramistVisibleCasesV6343" class="value">-</div></div>
                <div class="ceramistPreviewKpiV6342"><div class="label">Remake units</div><div id="remakeCeramistUnitsV6342" class="value">-</div></div>
                <div class="ceramistPreviewKpiV6342"><div class="label">Discount</div><div id="remakeCeramistDiscountV6342" class="value">-</div></div>
                <div class="ceramistPreviewKpiV6342" title="Percent of visible remake cases that are fully attributed to one confirmed responsible worker. Partial or unassigned cases are excluded from the numerator."><div class="label">Attribution</div><div id="remakeCeramistCoverageV6342" class="value">-</div></div>
              </div>
              <div class="ceramistPreviewMetaV6342">
                <div class="ceramistFilterStackV6343">
                  <div class="ceramistFilterRowV6343">
                    <div id="remakeCeramistCaseStatusBarV6343" class="ceramistFlagBarV6342">
                      <button class="ceramistFlagButtonV6342" data-ceramist-case-status="all" type="button" title="All cases" onclick="setCeramistCaseStatusV6343('all')">All <span id="remakeCeramistAllCountV6342">0</span></button>
                      <button class="ceramistFlagButtonV6342" data-ceramist-case-status="full" type="button" title="Fully attributed cases" onclick="setCeramistCaseStatusV6343('full')">Attributed <span id="remakeCeramistFullCountV6343">0</span></button>
                      <button class="ceramistFlagButtonV6342" data-ceramist-case-status="partial" type="button" title="Partially attributed cases" onclick="setCeramistCaseStatusV6343('partial')">Partial <span id="remakeCeramistPartialCountV6343">0</span></button>
                      <button class="ceramistFlagButtonV6342" data-ceramist-case-status="unattributed" type="button" title="Unattributed cases" onclick="setCeramistCaseStatusV6343('unattributed')">Unassigned <span id="remakeCeramistUnattributedCountV6342">0</span></button>
                    </div>
                    <span class="ceramistFilterSeparatorV6347" aria-hidden="true"></span>
                    <div id="remakeCeramistDiagnosticBarV6343" class="ceramistFlagBarV6342">
                      <button class="ceramistFlagButtonV6342" data-ceramist-diagnostic="none" type="button" title="No review flag filter" onclick="setCeramistDiagnosticV6343('none')">None</button>
                      <button class="ceramistFlagButtonV6342" data-ceramist-diagnostic="multi" type="button" title="Multiple remake chains" onclick="setCeramistDiagnosticV6343('multi')">Multi <span id="remakeCeramistMultiCountV6342">0</span></button>
                      <button class="ceramistFlagButtonV6342" data-ceramist-diagnostic="rootMissing" type="button" title="No matching prior/root CERAMICS completion was found in the attribution cache" onclick="setCeramistDiagnosticV6343('rootMissing')">No prior match <span id="remakeCeramistRootMissingCountV6342">0</span></button>
                      <button class="ceramistFlagButtonV6342" data-ceramist-diagnostic="noteEvidence" type="button" title="Prior or root invoice-note evidence" onclick="setCeramistDiagnosticV6343('noteEvidence')">Notes <span id="remakeCeramistNoteEvidenceCountV6343">0</span></button>
                    </div>
                  </div>
                </div>
              </div>
            </div>
            <div id="remakeCeramistWorkspaceV6343" class="ceramistWorkspaceV6343">
              <section class="ceramistPanelV6343" aria-label="Ceramist responsibility summary">
                <div class="ceramistPanelHeaderV6343"><div class="ceramistPanelTitleV6343">Responsible worker</div></div>
                <div class="remakeTableWrap ceramistPreviewTableWrapV6342"><table id="remakeCeramistTableV6342" class="remakeTable"></table></div>
              </section>
              <section class="ceramistPanelV6343" aria-label="Selected ceramist breakdown">
                <div class="ceramistPanelHeaderV6343"><div id="remakeCeramistDetailTitleV6343" class="ceramistPanelTitleV6343">Select a ceramist</div><div id="remakeCeramistBreakdownBarV6343" class="ceramistBreakdownBarV6343">
                  <button data-ceramist-breakdown="product" type="button" onclick="setCeramistBreakdownV6343('product')">Product</button>
                  <button data-ceramist-breakdown="group" type="button" onclick="setCeramistBreakdownV6343('group')">Group</button>
                  <button data-ceramist-breakdown="department" type="button" onclick="setCeramistBreakdownV6343('department')">Dept</button>
                  <button data-ceramist-breakdown="customer" type="button" onclick="setCeramistBreakdownV6343('customer')">Customer</button>
                  <button data-ceramist-breakdown="reason" type="button" onclick="setCeramistBreakdownV6343('reason')">Reason</button>
                </div></div>
                <div id="remakeCeramistDetailSelectionV6368" class="ceramistDetailSelectionV6368" hidden></div>
                <div class="remakeTableWrap ceramistPreviewTableWrapV6342"><table id="remakeCeramistDetailTableV6343" class="remakeTable"></table></div>
              </section>
            </div>
          </div>
        </div>
        <div class="remakePageFooterV6338" aria-label="Cache status">
          <div id="remakeCacheActionHomeV6338" aria-hidden="true"></div>
          <div class="remakeCacheActionBarV6338" aria-label="Remake cache controls">
            <button id="remakeReloadSavedBtn" class="remakeButton secondary" type="button" title="Reload the saved Drive cache and replace this browser copy" onclick="reloadRemakeFactorSavedCache()"><span class="remakeCacheActionIconV6338" aria-hidden="true">&#8595;</span><span class="remakeCacheActionLabelV6338">Reload</span></button>
            <button id="remakeRefreshBtn" class="remakeButton" type="button" title="Build missing months first, then replace current month from the API" onclick="refreshRemakeFactorData()"><span class="remakeCacheActionIconV6338" aria-hidden="true">&#8635;</span><span class="remakeCacheActionLabelV6338">Refresh</span></button>
          </div>
          <span id="remakeCeramistStatusV6342" class="remakeRefreshStamp remakeFooterStampV6338 remakeWorkerFooterStampV6367" title="This is when the worker cache was rebuilt, not the latest case invoice date.">Worker cache: not loaded</span>
          <span id="remakeLastRefresh" class="remakeRefreshStamp remakeFooterStampV6338 remakeMainFooterStampV6367">${escV6230(uiV6230.lastStamp || 'Not loaded')}</span>
        </div>
      </div>`;
    syncFilterControlsV6230();
  }

  function setNoticeV6230(message, tone) {
    const el = idV6230('remakeNotice');
    if (!el) return;
    el.className = 'remakeNotice' + (message ? ' active' : '') + (tone ? ' ' + tone : '');
    el.textContent = message || '';
  }
  function setLoadingV6230(value) {
    const btn = idV6230('remakeRefreshBtn');
    if (btn) btn.disabled = !!value;
    const reloadBtn = idV6230('remakeReloadSavedBtn');
    if (reloadBtn) reloadBtn.disabled = !!value;
    if (!window.remakeFactorState) window.remakeFactorState = {};
    window.remakeFactorState.loading = !!value;
  }
  function setStampV6230(text) { uiV6230.lastStamp = text || ''; const el = idV6230('remakeLastRefresh'); if (el) el.textContent = uiV6230.lastStamp; }
  function setPerfV6230(ms, filteredCount, totalCount) {
    const el = idV6230('remakePerfV6230');
    if (el) el.textContent = 'Render ' + fmtCountV6230(ms) + ' ms · Rows ' + fmtCountV6230(filteredCount) + ' / ' + fmtCountV6230(totalCount);
  }

  function monthKeyV6230(rawValue) {
    const raw = textV6230(rawValue, '');
    if (/^\d{4}-\d{2}/.test(raw)) return raw.slice(0, 7);
    if (/^\d{8}$/.test(raw)) return raw.slice(0, 4) + '-' + raw.slice(4, 6);
    if (/^\d{1,2}\/\d{1,2}\/\d{4}/.test(raw)) {
      const d = new Date(raw);
      if (!isNaN(d.getTime())) return d.getFullYear() + '-' + String(d.getMonth() + 1).padStart(2, '0');
    }
    const d = new Date(raw);
    if (raw && !isNaN(d.getTime())) return d.getFullYear() + '-' + String(d.getMonth() + 1).padStart(2, '0');
    return raw || 'Unknown';
  }
  function monthLabelV6230(key) {
    const m = /^(\d{4})-(\d{2})$/.exec(textV6230(key, ''));
    if (!m) return textV6230(key, 'Unknown');
    return new Date(Number(m[1]), Number(m[2]) - 1, 1).toLocaleString(undefined, { month: 'short', year: '2-digit' });
  }
  function sortMonthV6230(a, b) { return String(a).localeCompare(String(b), undefined, { numeric: true, sensitivity: 'base' }); }

  function isLabLocationV6230(value) {
    const v = textV6230(value, '').toLowerCase().replace(/[^a-z0-9]+/g, ' ').trim();
    return !v || v === 'pacifica' || v === 'san ramon' || v === 'deanza' || v === 'de anza' || v === 'cada' || v === 'cad' || v === 'cda' || v === 'case level';
  }
  function inferDepartmentFromProductNameV6230(productName) {
    const name = textV6230(productName, '');
    const first = textV6230(name.split(' - ')[0], '');
    const allowed = new Set(['Fixed','Implant','Alloy','Removable','Nightguard','Advanced Prosthetics','Denture Services','Services','Shipping','CDA Connect']);
    return allowed.has(first) ? first : '';
  }
  function isPlaceholderDepartmentV6264(value) {
    const clean = textV6230(value, '').toLowerCase();
    return !clean || clean === 'unassigned' || clean === 'unknown' || clean === 'not specified' || clean === 'case level';
  }
  function inferDepartmentFromGroupCodeV6273(value) {
    const clean = textV6230(value, '');
    const lower = clean.toLowerCase();
    if (!clean || lower === 'unassigned' || lower === 'unknown' || lower === 'not specified') return '';
    if (lower === 'rem' || lower === 'removable' || lower.indexOf('removable') === 0) return 'Removable';
    if (lower === 'ng' || lower === 'nightguard' || lower === 'night guard' || lower.indexOf('nightguard') === 0 || lower.indexOf('night guard') === 0) return 'Nightguard';
    if (lower === 'ap' || lower.indexOf('ap-') === 0 || lower.indexOf('advanced prosthetics') === 0) return 'Advanced Prosthetics';
    if (lower === 'fixed' || lower.indexOf('fixed') === 0) return 'Fixed';
    if (lower === 'implant' || lower.indexOf('implant') === 0) return 'Implant';
    if (lower === 'alloy' || lower.indexOf('alloy') === 0) return 'Alloy';
    if (lower.indexOf('denture') === 0) return 'Denture Services';
    if (lower === 'shipping' || lower.indexOf('ship') === 0) return 'Shipping';
    if (lower === 'cda connect' || lower.indexOf('cda connect') === 0) return 'CDA Connect';
    if (lower === 'services' || lower.indexOf('service') === 0) return 'Services';
    return '';
  }
  function inferProductGroupV6264(row, productMeta, productName, department) {
    const candidates = [
      row && row.taxGroup,
      row && row.productGroup,
      row && row.productsGroup,
      row && row.Products_Group,
      row && row.group,
      productMeta && productMeta.group,
      row && row.taxCategory,
      row && row.productCategory,
      row && row.productsCategory,
      row && row.Products_Category,
      productMeta && productMeta.category
    ];
    for (const candidate of candidates) {
      const clean = textV6230(candidate, '');
      if (clean && clean.toLowerCase() !== 'unassigned' && clean !== department) return clean;
    }
    const parts = textV6230(productName, '').split(' - ').map(part => textV6230(part, '')).filter(Boolean);
    if (parts.length >= 2) return parts[0];
    return department || 'Unassigned';
  }

  function overviewLookupsV6230() {
    const customerById = new Map();
    const productById = new Map();
    const productByName = new Map();
    const sourceFacts = typeof facts !== 'undefined' && Array.isArray(facts) ? facts : [];
    sourceFacts.forEach(row => {
      const cid = textV6230(row.customerId, '');
      if (cid && !customerById.has(cid)) {
        const metaName = textV6230(row.customerName || row.customerFullName || row.Customers_CustomerFullName, '');
        const metaPractice = textV6230(row.practiceName || row.customerPracticeName || row.Customers_PracticeName, '');
        customerById.set(cid, {
          id: cid,
          name: metaName && metaName !== cid ? metaName : '',
          practiceName: metaPractice && metaPractice !== cid ? metaPractice : '',
          active: row.customerActive === false ? false : true
        });
      } else if (cid && customerById.has(cid) && row.customerActive === false) customerById.get(cid).active = false;
      const pid = textV6230(row.productId, '');
      const pname = textV6230(row.productsDescription, pid || 'Unknown product');
      const dept = textV6230(row.productsDepartment, '');
      const meta = { id: pid, name: pname, department: dept, group: textV6230(row.productGroup || row.productsGroup || row.Products_Group || row.group || row.category || row.productsCategory || row.Products_Category, '') };
      if (pid && !productById.has(pid)) productById.set(pid, meta);
      const nameKey = pname.toLowerCase();
      if (nameKey && !productByName.has(nameKey)) productByName.set(nameKey, meta);
    });
    return { customerById, productById, productByName, signature: sourceFacts.length };
  }

  function isRemakeV6230(row) {
    if (!row) return false;
    if (row.isRemake === true) return true;
    if (row.isRemake === false) return false;
    if (numV6230(row.remakeCases) > 0 || numV6230(row.remakeUnits) > 0) return true;
    const flag = textV6230(row.remakeFlag || row.remake || row.caseProductsRemake || row.CaseProducts_Remake, '').toLowerCase();
    return /^(y|yes|true|1|r|remake)$/.test(flag);
  }
  function caseIdV6230(row) { return textV6230(row && (row.caseId || row.caseID || row.caseNumber || row.Cases_CaseNumber || row.case), ''); }

  function rawRowsV6230() {
    const st = window.remakeFactorState || {};
    const data = st.data || {};
    if (Array.isArray(st.rows) && st.rows.length) return st.rows;
    if (Array.isArray(data.detailRows) && data.detailRows.length) return data.detailRows;
    if (Array.isArray(data.rows) && data.rows.length) return data.rows;
    return [];
  }

  function normalizeRowsV6230(rows, lookups) {
    lookups = lookups || overviewLookupsV6230();
    return (rows || []).map(row => {
      const month = monthKeyV6230(row.month || row.invoiceMonth || row.Cases_InvoiceDate || row.invoiceDate || row.dateIn || row.modifyDate || row.Cases_CreateDate);
      const year = row.year || (/^\d{4}/.test(month) ? Number(month.slice(0, 4)) : '');
      const remake = isRemakeV6230(row);
      const qty = numV6230(row.quantity !== undefined ? row.quantity : (row.units !== undefined ? row.units : row.CaseProducts_Quantity));
      const rawProductId = textV6230(row.productId || row.productID || row.CaseProducts_ProductID, '');
      const rawProductName = textV6230(row.productName || row.productsDescription || row.Products_Description || row.productDescription || row.invoiceDescription || rawProductId, 'Unknown product');
      const productMeta = (rawProductId && lookups.productById.get(rawProductId)) || lookups.productByName.get(rawProductName.toLowerCase()) || null;
      const productName = textV6230(productMeta && productMeta.name, rawProductName);
      const productKey = rawProductId || productName;
      let department = '';
      const inferredDepartment = inferDepartmentFromProductNameV6230(productName);
      const rawGroupForDepartment = row.taxGroup || row.productGroup || row.productsGroup || row.Products_Group || row.group || row.taxCategory || (productMeta && (productMeta.group || productMeta.category)) || '';
      const inferredGroupDepartment = inferDepartmentFromGroupCodeV6273(rawGroupForDepartment);
      const deptCandidates = [
        row.taxDepartment,
        row.taxDept,
        row.Products_Department,
        row.productsDepartment,
        row.productDepartment,
        productMeta && productMeta.department,
        inferredGroupDepartment,
        inferredDepartment,
        row.department
      ];
      for (const candidate of deptCandidates) {
        const clean = textV6230(candidate, '');
        if (clean && !isLabLocationV6230(clean) && !isPlaceholderDepartmentV6264(clean)) { department = clean; break; }
      }
      department = department || inferredDepartment || 'Unassigned';
      const productGroup = inferProductGroupV6264(row, productMeta, productName, department);
      const rawCustomerId = textV6230(row.customerId || row.customerID || row.Cases_CustomerID, '');
      const customerMeta = rawCustomerId ? lookups.customerById.get(rawCustomerId) : null;
      function firstNonIdV6253(values) {
        for (const value of values || []) {
          const clean = textV6230(value, '');
          if (!clean) continue;
          if (rawCustomerId && clean === rawCustomerId) continue;
          return clean;
        }
        return '';
      }
      const rawCustomerName = firstNonIdV6253([
        customerMeta && customerMeta.name,
        row.customerFullName,
        row.Customers_CustomerFullName,
        row.customersCustomerFullName,
        row.customerDisplayName,
        row.customerName
      ]);
      const rawPracticeName = firstNonIdV6253([
        customerMeta && customerMeta.practiceName,
        row.practiceName,
        row.customerPracticeName,
        row.Customers_PracticeName,
        row.customersPracticeName,
        row.practice,
        row.officeName
      ]);
      const customerName = textV6230(rawCustomerName, rawPracticeName || rawCustomerId || 'Unknown customer');
      const practiceName = rawPracticeName;
      const customerDisplayLabel = customerLabelV6246({ customerId: rawCustomerId, customerName: rawCustomerName || customerName, practiceName: practiceName });
      const customerActive = customerMeta ? customerMeta.active !== false : (row.customerActive === false || row.customerActive === 'false' ? false : true);
      return {
        month,
        year,
        caseId: caseIdV6230(row),
        caseNumber: textV6230(row.caseNumber || row.caseNo || row.Cases_CaseNumber, ''),
        caseProductLineId: textV6230(
          row.caseProductLineId || row.caseProductLineID || row.caseProductId || row.caseProductID ||
          row.productLineId || row.productLineID || row.lineId || row.lineID,
          ''
        ),
        customerId: rawCustomerId || customerName,
        customerKey: rawCustomerId || customerName,
        customerName,
        practiceName,
        customerDisplayLabel,
        customerActive,
        department,
        productId: rawProductId,
        productKey,
        productName,
        productGroup,
        remakeReason: remake ? textV6230(row.remakeReason || row.reason || row.CaseProducts_RemakeReason, 'Not specified') : 'Not a remake',
        quantity: qty,
        units: qty,
        isRemake: remake,
        remakeUnits: remake ? numV6230(row.remakeUnits !== undefined ? row.remakeUnits : qty) : 0,
        remakeDiscount: remake ? Math.abs(numV6230(row.remakeDiscount || row.CaseProducts_RemakeDiscount || 0)) : 0,
        invoiceDate: textV6230(row.invoiceDate || row.Cases_InvoiceDate, ''),
        caseStatus: textV6230(row.caseStatus || row.status || row.Cases_Status, ''),
        invoiceDateTryIn: textV6230(row.invoiceDateTryIn || row.Cases_InvoiceDateTryIn, ''),
        isRealInvoicedCharge: row.isRealInvoicedCharge !== false,
        creditDebitReason: textV6230(row.creditDebitReason || row.Cases_CreditDebitReason, ''),
        chargeAmount: numV6230(row.chargeAmount !== undefined ? row.chargeAmount : row.totalCharge),
        creditAmount: numV6230(row.creditAmount || 0),
        netAmount: numV6230(row.netAmount !== undefined ? row.netAmount : row.totalCharge),
        salesLogicClass: textV6230(row.salesLogicClass, 'charge'),
        remakeDiscountSource: textV6230(row.remakeDiscountSource, '')
      };
    });
  }

  function customerLabelV6246(row) {
    const cid = textV6230(row && (row.customerId || row.customerKey), '');
    const name = textV6230(row && (row.customerName || row.customerDisplayName), '');
    const practice = textV6230(row && (row.practiceName || row.customerPracticeName), '');
    const cleanName = name && cid && name === cid ? '' : name;
    const cleanPractice = practice && cid && practice === cid ? '' : practice;
    let label = '';
    if (cleanName && cleanPractice && cleanName !== cleanPractice) label = cleanName + ' - ' + cleanPractice;
    else label = cleanName || cleanPractice || cid || 'Unknown customer';
    if (cid && label !== cid && label.indexOf('(' + cid + ')') < 0) label += ' (' + cid + ')';
    return label;
  }

  function normalizedRowsV6230() {
    const st = window.remakeFactorState || {};
    const rawRows = rawRowsV6230();
    const overviewSignature = typeof facts !== 'undefined' && Array.isArray(facts) ? facts.length : 0;
    const sig = String(rawRows.length) + '|' + String(overviewSignature);
    if (Array.isArray(st.normalizedRowsV6230) && st.normalizedRowsSignatureV6230 === sig) return st.normalizedRowsV6230;
    const lookups = overviewLookupsV6230();
    const rows = normalizeRowsV6230(rawRows, lookups);
    st.normalizedRowsV6230 = rows;
    st.normalizedRowsSignatureV6230 = sig;
    window.remakeFactorState = st;
    return rows;
  }

  function summarizeV6230(rows) {
    const cases = new Set();
    const remakeCases = new Set();
    let totalFallback = 0;
    let remakeFallback = 0;
    let totalUnits = 0;
    let remakeUnits = 0;
    let remakeDiscount = 0;
    (rows || []).forEach(row => {
      const cid = caseIdV6230(row);
      if (cid) cases.add(cid); else totalFallback += numV6230(row.totalCases) || 1;
      totalUnits += numV6230(row.quantity !== undefined ? row.quantity : row.units);
      if (isRemakeV6230(row)) {
        if (cid) remakeCases.add(cid); else remakeFallback += numV6230(row.remakeCases) || 1;
        remakeUnits += numV6230(row.remakeUnits !== undefined ? row.remakeUnits : (row.quantity !== undefined ? row.quantity : row.units));
        remakeDiscount += Math.abs(numV6230(row.remakeDiscount));
      }
    });
    const totalCases = cases.size + totalFallback;
    const remakeCaseCount = remakeCases.size + remakeFallback;
    return {
      totalCases,
      remakeCases: remakeCaseCount,
      totalUnits,
      remakeUnits,
      remakeDiscount,
      remakeCaseRate: totalCases ? remakeCaseCount / totalCases : 0,
      remakeUnitRate: totalUnits ? remakeUnits / totalUnits : 0
    };
  }

  function groupV6230(rows, keyGetter, labelGetter, extraReducer) {
    const groups = new Map();
    (rows || []).forEach(row => {
      const key = textV6230(keyGetter(row), 'Not specified');
      if (!groups.has(key)) groups.set(key, { key, label: textV6230(labelGetter ? labelGetter(row) : key, key), rows: [] });
      const group = groups.get(key);
      group.rows.push(row);
      if (extraReducer) extraReducer(group, row);
    });
    return Array.from(groups.values()).map(group => Object.assign(group, summarizeV6230(group.rows)));
  }

  function syncFiltersFromControlsV6230() {
    Object.keys(filterMetaV6245).forEach(kind => {
      const meta = filterMetaV6245[kind];
      const el = idV6230(meta.inputId);
      if (!el) return;
      try {
        const parsed = JSON.parse(el.value || '[]');
        if (!(excludedFilterValuesV6389(kind).length && Array.isArray(parsed) && !parsed.length)) setFilterValuesV6245(kind, parsed);
      } catch (error) {
        if (!excludedFilterValuesV6389(kind).length) setFilterValuesV6245(kind, []);
      }
    });
    writeLocalJsonV6230(FILTERS_KEY_V6230, uiV6230.filters);
  }
  function syncFilterControlsV6230() {
    Object.keys(filterMetaV6245).forEach(kind => {
      const meta = filterMetaV6245[kind];
      const el = idV6230(meta.inputId);
      if (el) el.value = JSON.stringify(filterValuesV6245(kind));
      renderDropdownButtonV6245(kind);
      const panel = idV6230(meta.panelId);
      if (panel && panel.classList.contains('open')) window.renderRemakeDropdownListV6245(kind);
    });
  }
  function currentFiltersV6230() { syncFiltersFromControlsV6230(); return uiV6230.filters; }
  function rowsForFiltersV6230(rows, options) {
    const includeMonths = !options || options.months !== false;
    const includeYear = !(options && options.year === false);
    const includeDepartment = !(options && options.department === false);
    const includeProduct = !(options && options.product === false);
    const includeProductGroup = !(options && options.productGroup === false);
    const includeCustomer = !(options && options.customer === false);
    const includeReason = !(options && options.reason === false);
    const selectedMonths = uiV6230.selectedMonths || new Set();

    /* v6.333: compile filter membership once per pass. The prior predicate
       rebuilt the selected arrays for every field on every row, which caused
       a visible main-thread pause before cross-filter transitions. */
    const compiledFiltersV6333 = {};
    ['year','department','product','productGroup','customer','reason'].forEach(function(kind) {
      const values = filterValuesV6245(kind).map(String);
      const excluded = excludedFilterValuesV6389(kind).map(String);
      compiledFiltersV6333[kind] = {
        none: values.length === 1 && values[0] === REMAKE_NONE_FILTER_VALUE_V6308,
        values: values.length ? new Set(values) : null,
        excluded: excluded.length ? new Set(excluded) : null
      };
    });
    function matchesCompiledFilterV6333(kind, value) {
      const filter = compiledFiltersV6333[kind];
      if (!filter || filter.none) return false;
      const text = String(value);
      if (filter.values) return filter.values.has(text);
      return !filter.excluded || !filter.excluded.has(text);
    }

    return (rows || []).filter(row =>
      (!includeYear || matchesCompiledFilterV6333('year', row.year)) &&
      (!includeDepartment || matchesCompiledFilterV6333('department', row.department)) &&
      (!includeProduct || matchesCompiledFilterV6333('product', row.productKey)) &&
      (!includeProductGroup || matchesCompiledFilterV6333('productGroup', row.productGroup || row.department || 'Unassigned')) &&
      (!includeCustomer || matchesCompiledFilterV6333('customer', row.customerKey)) &&
      (!includeReason || matchesCompiledFilterV6333('reason', row.remakeReason)) &&
      (!includeMonths || !selectedMonths.size || selectedMonths.has(row.month))
    );
  }

  /* v6.252 denominator logic:
     Year/month, department, product, and customer filters define the full case base.
     Reason filters are remake-only, so they narrow the numerator but never shrink Total Cases. */
  function rowsForBaseFiltersV6252(rows, options) {
    return rowsForFiltersV6230(rows, Object.assign({}, options || {}, { reason: false }));
  }
  function rowsForRemakeFiltersV6252(rows, options) {
    return rowsForFiltersV6230(rows, options || {});
  }
  function summarizeSplitV6252(totalRows, remakeRows) {
    const cases = new Set();
    let totalFallback = 0;
    let totalUnits = 0;
    (totalRows || []).forEach(row => {
      const cid = caseIdV6230(row);
      if (cid) cases.add(cid); else totalFallback += numV6230(row.totalCases) || 1;
      totalUnits += numV6230(row.quantity !== undefined ? row.quantity : row.units);
    });

    const remakeCases = new Set();
    let remakeFallback = 0;
    let remakeUnits = 0;
    let remakeDiscount = 0;
    (remakeRows || []).filter(isRemakeV6230).forEach(row => {
      const cid = caseIdV6230(row);
      if (cid) remakeCases.add(cid); else remakeFallback += numV6230(row.remakeCases) || 1;
      remakeUnits += numV6230(row.remakeUnits !== undefined ? row.remakeUnits : (row.quantity !== undefined ? row.quantity : row.units));
      remakeDiscount += Math.abs(numV6230(row.remakeDiscount));
    });

    const totalCases = cases.size + totalFallback;
    const remakeCaseCount = remakeCases.size + remakeFallback;
    return {
      totalCases,
      remakeCases: remakeCaseCount,
      totalUnits,
      remakeUnits,
      remakeDiscount,
      remakeCaseRate: totalCases ? remakeCaseCount / totalCases : 0,
      remakeUnitRate: totalUnits ? remakeUnits / totalUnits : 0
    };
  }
  function groupSplitV6252(totalRows, remakeRows, keyGetter, labelGetter, extraReducer) {
    const groups = new Map();
    function ensureGroup(row) {
      const key = textV6230(keyGetter(row), 'Not specified');
      if (!groups.has(key)) groups.set(key, { key, label: textV6230(labelGetter ? labelGetter(row) : key, key), rows: [], totalRows: [], remakeRows: [] });
      const group = groups.get(key);
      if (extraReducer) extraReducer(group, row);
      return group;
    }
    (totalRows || []).forEach(row => {
      const group = ensureGroup(row);
      group.rows.push(row);
      group.totalRows.push(row);
    });
    (remakeRows || []).filter(isRemakeV6230).forEach(row => {
      const group = ensureGroup(row);
      group.remakeRows.push(row);
    });
    return Array.from(groups.values()).map(group => Object.assign(group, summarizeSplitV6252(group.totalRows, group.remakeRows)));
  }
  function monthlyGroupsSplitV6252(totalRows, remakeRows) {
    return groupSplitV6252(totalRows, remakeRows, row => row.month || 'Unknown', row => row.month || 'Unknown')
      .filter(row => row.key !== 'Unknown')
      .sort((a, b) => sortMonthV6230(a.key, b.key))
      .slice(-24);
  }
  function reasonFilterActiveV6252() {
    return filterValuesV6245('reason').length > 0 || excludedFilterValuesV6389('reason').length > 0;
  }
  function remakeCasesHeaderV6252() {
    return reasonFilterActiveV6252() ? 'Reason Remakes' : 'Remake Cases';
  }
  function remakeRateHeaderV6252() {
    return reasonFilterActiveV6252() ? 'Reason Rate' : 'Case Rate';
  }
  function normalizeOptionsV6245(options) { const seen = new Set(); const clean = []; (options || []).forEach(opt => { const value = textV6230(opt && opt.value !== undefined ? opt.value : opt, ''); const label = textV6230(opt && opt.label !== undefined ? opt.label : opt, value); if (!value || seen.has(value)) return; seen.add(value); clean.push({ value, label, active: opt && opt.active }); }); return clean.sort((a,b)=>a.label.localeCompare(b.label,undefined,{numeric:true,sensitivity:'base'})); }
  function fillSelectV6230(id, allLabel, options, selected) {
    const map={remakeYearFilter:'year',remakeDepartmentFilter:'department',remakeProductFilter:'product',remakeCustomerFilter:'customer',remakeReasonFilter:'reason'};
    const kind=map[id];
    if (!kind) return;
    uiV6230.filterOptionsV6245[kind]=normalizeOptionsV6245(options);
    renderDropdownButtonV6245(kind);
    const meta=filterMetaV6245[kind];
    const panel=meta?idV6230(meta.panelId):null;
    if(panel&&panel.classList.contains('open')) window.renderRemakeDropdownListV6245(kind);
  }
  function populateFiltersV6230(rows) {
    // v6.300: filter inventories are based on the full normalized data set, not
    // the current cross-filter. Rebuilding thousands of hidden dropdown rows on
    // every table click was the main source of the visible freeze/twitch.
    if (uiV6230.filterOptionRowsV6300 === rows && uiV6230.filterOptionsV6245) {
      syncFilterControlsV6230();
      writeLocalJsonV6230(FILTERS_KEY_V6230, uiV6230.filters);
      return;
    }
    fillSelectV6230('remakeYearFilter','All years',Array.from(new Set((rows||[]).map(row=>row.year).filter(Boolean))).sort((a,b)=>Number(b)-Number(a)).map(year=>({value:String(year),label:String(year)})),uiV6230.filters.year);
    fillSelectV6230('remakeDepartmentFilter','All departments',Array.from(new Set((rows||[]).map(row=>row.department).filter(Boolean))).map(dept=>({value:dept,label:dept})),uiV6230.filters.department);
    fillSelectV6230('remakeProductFilter','All products',groupV6230(rows,row=>row.productKey,row=>row.productName).map(row=>({value:row.key,label:row.label})),uiV6230.filters.product);
    uiV6230.filterOptionsV6245.productGroup = normalizeOptionsV6245(groupV6230(rows,row=>row.productGroup || row.department || 'Unassigned',row=>row.productGroup || row.department || 'Unassigned').map(row=>({value:row.key,label:row.label})));
    fillSelectV6230('remakeCustomerFilter','All customers',groupV6230(rows,row=>row.customerKey,row=>customerLabelV6246(row),(group,row)=>{group.customerId=row.customerId||row.customerKey; group.customerName=row.customerName||row.customerKey; group.practiceName=row.practiceName||''; group.customerDisplayLabel=customerLabelV6246(row); if(row.customerActive===false) group.active=false;}).map(row=>({value:row.key,label:row.label,active:row.active!==false})),uiV6230.filters.customer);
    fillSelectV6230('remakeReasonFilter','All reasons',Array.from(new Set((rows||[]).filter(isRemakeV6230).map(row=>row.remakeReason||'Not specified'))).map(reason=>({value:reason,label:reason})),uiV6230.filters.reason);
    uiV6230.filterOptionRowsV6300 = rows;
    syncFilterControlsV6230(); writeLocalJsonV6230(FILTERS_KEY_V6230, uiV6230.filters);
  }
  function filterLabelV6230(kind,value){ if(!value) return ''; const opt=filterOptionLabelV6245(kind,value); if(opt) return opt; return String(value); }
  function pillKindLabelV6247(kind) {
    return kind === 'year' ? 'Year' : kind === 'department' ? 'Department' : kind === 'product' ? 'Product' : kind === 'productGroup' ? 'Group' : kind === 'customer' ? 'Customer' : kind === 'reason' ? 'Reason' : 'Month';
  }
  function compactPillLabelV6247(kind, values, labelGetter) {
    const labels = (values || []).map(value => labelGetter ? labelGetter(value) : String(value)).filter(Boolean);
    if (!labels.length) return '';
    const shown = labels.slice(0, 2).join(', ');
    const extra = labels.length > 2 ? ' +' + (labels.length - 2) : '';
    return pillKindLabelV6247(kind) + ': ' + shown + extra;
  }
  function renderPillsV6230(){
    const rail = idV6230('remakePillsV6230');
    if (!rail) return;
    const pills = [];
    ['year','department','product','productGroup','customer','reason'].forEach(kind => {
      const excluded = excludedFilterValuesV6389(kind);
      const values = filterValuesV6245(kind);
      if (excluded.length) {
        const fullTitle = excluded.map(value => pillKindLabelV6247(kind) + ' excluded: ' + filterLabelV6230(kind, value)).join('\n');
        const labels = excluded.map(value => filterLabelV6230(kind, value)).filter(Boolean);
        const shown = labels.slice(0, 2).join(', ');
        const extra = labels.length > 2 ? ' +' + (labels.length - 2) : '';
        pills.push({ clear: kind, label: pillKindLabelV6247(kind) + ' excluded: ' + shown + extra, title: fullTitle });
        return;
      }
      if (!values.length) return;
      const fullTitle = values.map(value => pillKindLabelV6247(kind) + ': ' + filterLabelV6230(kind, value)).join('\n');
      const label = compactPillLabelV6247(kind, values, value => filterLabelV6230(kind, value));
      pills.push({ clear: kind, label, title: fullTitle });
    });
    const months = Array.from(uiV6230.selectedMonths || []).sort(sortMonthV6230);
    if (months.length) {
      pills.push({
        clear: 'months',
        label: compactPillLabelV6247('month', months, month => monthLabelV6230(month)),
        title: months.map(month => 'Month: ' + monthLabelV6230(month)).join('\n')
      });
    }
    if (!pills.length) { rail.innerHTML = ''; return; }
    rail.innerHTML = pills.map(pill => '<span class="remakePillV6230" title="' + escV6230(pill.title || pill.label) + '"><span>' + escV6230(pill.label) + '</span><button type="button" onclick="clearRemakeFilterV6230(\'' + escJsV6230(pill.clear) + '\')">x</button></span>').join('') + '<button type="button" class="remakeClearAllV6230" title="Reset all filters" aria-label="Reset all filters" onclick="clearRemakeFilterV6230(\'all\')">Reset all</button>';
  }
  function allOptionsForDropdownV6245(kind){ return ((uiV6230.filterOptionsV6245&&uiV6230.filterOptionsV6245[kind])||[]).slice(); }
  function allOptionValuesForDropdownV6245(kind){ return allOptionsForDropdownV6245(kind).map(opt=>String(opt.value)); }
  function explicitSelectionSetV6250(kind){ return new Set(filterValuesV6245(kind).map(value=>String(value)).filter(value=>value!==REMAKE_NONE_FILTER_VALUE_V6308)); }
  function exclusionSetV6389(kind){ return new Set(excludedFilterValuesV6389(kind).map(String)); }
  function effectiveSelectionSetV6250(kind){ if(filterIsNoneV6308(kind)) return new Set(); const selected=explicitSelectionSetV6250(kind); if(selected.size) return selected; const all=new Set(allOptionValuesForDropdownV6245(kind)); exclusionSetV6389(kind).forEach(value=>all.delete(value)); return all; }
  function normalizeSelectionForSaveV6250(kind, values){ const raw=(values||[]).map(value=>String(value)); const nonNone=raw.filter(value=>value!==REMAKE_NONE_FILTER_VALUE_V6308); if(raw.indexOf(REMAKE_NONE_FILTER_VALUE_V6308)>=0&&!nonNone.length) return [REMAKE_NONE_FILTER_VALUE_V6308]; const all=allOptionValuesForDropdownV6245(kind); const allSet=new Set(all); const clean=[]; const seen=new Set(); nonNone.forEach(text=>{ if(!allSet.has(text)||seen.has(text)) return; seen.add(text); clean.push(text); }); if(!clean.length) return []; if(clean.length===all.length) return []; return clean; }
  function commitEffectiveSelectionV6389(kind, values){ const all=allOptionValuesForDropdownV6245(kind); const allSet=new Set(all); const next=new Set(); (values||[]).forEach(value=>{ const text=String(value); if(allSet.has(text)) next.add(text); }); if(!next.size){ setFilterValuesV6245(kind,[REMAKE_NONE_FILTER_VALUE_V6308]); return; } if(next.size===all.length){ setFilterValuesV6245(kind,[]); return; } const excluded=all.filter(value=>!next.has(value)); if(excluded.length < next.size) setFilterExclusionsV6389(kind,excluded); else setFilterValuesV6245(kind,Array.from(next)); }
  function visibleOptionsForDropdownV6245(kind){
    const meta=filterMetaV6245[kind];
    const search=meta?textV6230((idV6230(meta.searchId)||{}).value,''):'';
    let options=allOptionsForDropdownV6245(kind);
    if(!search) return options;
    return options.map((opt,index)=>({
      opt,
      index,
      fuzzyScore:window.dashboardFuzzySearchV6412.score(
        [opt&&opt.label,opt&&opt.value,meta&&meta.label].filter(Boolean).join(' '),
        search
      )
    })).filter(entry=>entry.fuzzyScore>0).sort((a,b)=>b.fuzzyScore-a.fuzzyScore||a.index-b.index).map(entry=>entry.opt);
  }
  function syncVisibleRemakeToggleV6502(kind, visible, effective) {
    const input = idV6230('remakeVisibleToggleV6502_' + kind);
    if (!input) return;
    const shown = Array.isArray(visible) ? visible : visibleOptionsForDropdownV6245(kind);
    const selected = effective instanceof Set ? effective : effectiveSelectionSetV6250(kind);
    const selectedVisible = shown.reduce(function(total, opt){ return total + (selected.has(String(opt.value)) ? 1 : 0); }, 0);
    input.disabled = shown.length === 0;
    input.checked = shown.length > 0 && selectedVisible === shown.length;
    input.indeterminate = selectedVisible > 0 && selectedVisible < shown.length;
    input.setAttribute('aria-checked', input.indeterminate ? 'mixed' : (input.checked ? 'true' : 'false'));
    input.title = input.checked
      ? 'Uncheck to deselect all ' + shown.length + ' visible options'
      : 'Check to select all ' + shown.length + ' visible options';
  }
  function renderDropdownButtonV6245(kind){
    const meta=filterMetaV6245[kind];
    if(!meta) return;
    const button=idV6230(meta.buttonId);
    const count=idV6230(meta.countId);
    const values=filterValuesV6245(kind);
    const excluded=excludedFilterValuesV6389(kind);
    const visible=visibleOptionsForDropdownV6245(kind);
    const none=filterIsNoneV6308(kind);
    const effective=effectiveSelectionSetV6250(kind);
    const selectedCount=effective.size;
    if(button){
      if(none) button.textContent='None';
      else if(excluded.length) button.textContent='All except '+excluded.length;
      else if(!values.length) button.textContent=meta.allLabel;
      else if(values.length===1) button.textContent=filterOptionLabelV6245(kind,values[0])||values[0];
      else button.textContent=values.length+' selected';
    }
    if(count) count.textContent=none?('None selected · '+visible.length+' shown'):(excluded.length?(selectedCount+' selected · '+excluded.length+' excluded · '+visible.length+' shown'):(values.length?(values.length+' selected · '+visible.length+' shown'):('All selected · '+visible.length+' shown')));
    syncVisibleRemakeToggleV6502(kind, visible, effective);
  }
  window.renderRemakeDropdownListV6245=function(kind){ const meta=filterMetaV6245[kind]; if(!meta) return; const list=idV6230(meta.listId); if(!list) return; const explicit=explicitSelectionSetV6250(kind); const excluded=exclusionSetV6389(kind); const effective=effectiveSelectionSetV6250(kind); const visible=visibleOptionsForDropdownV6245(kind); renderDropdownButtonV6245(kind); if(!visible.length){list.innerHTML='<div class="remakeDropdownEmptyV6245">No matching options.</div>';return;} list.innerHTML=visible.map(opt=>{ const value=String(opt.value); const checked=effective.has(value); const explicitActive=!excluded.size&&explicit.size&&checked; const inactive=opt.active===false?'<span class="inactiveBadge">Inactive</span>':''; return '<div class="remakeDropdownRowV6245'+(explicitActive?' active':'')+'" onclick="toggleRemakeMultiValueV6245(\''+escJsV6230(kind)+'\',\''+escJsV6230(value)+'\')"><input type="checkbox" '+(checked?'checked':'')+'><div class="remakeDropdownLabelV6245" title="'+escV6230(opt.label)+'">'+escV6230(opt.label)+inactive+'</div><button type="button" class="remakeDropdownOnlyV6245" onclick="onlyRemakeMultiValueV6245(\''+escJsV6230(kind)+'\',\''+escJsV6230(value)+'\',event)">Only</button></div>'; }).join(''); };
  function renderAllRemakeDropdownsV6245(){ Object.keys(filterMetaV6245).forEach(kind=>{ const meta=filterMetaV6245[kind]; renderDropdownButtonV6245(kind); const panel=meta?idV6230(meta.panelId):null; if(panel&&panel.classList.contains('open')) window.renderRemakeDropdownListV6245(kind); }); }
  window.toggleRemakeDropdownV6245=function(kind){ const meta=filterMetaV6245[kind]; if(!meta) return; Object.keys(filterMetaV6245).forEach(other=>{const m=filterMetaV6245[other]; const p=idV6230(m.panelId); const b=idV6230(m.buttonId); if(other!==kind){ if(p) p.classList.remove('open'); if(b) b.classList.remove('active'); }}); const panel=idV6230(meta.panelId); const btn=idV6230(meta.buttonId); if(panel) panel.classList.toggle('open'); if(btn) btn.classList.toggle('active'); window.renderRemakeDropdownListV6245(kind); const search=idV6230(meta.searchId); if(panel&&panel.classList.contains('open')&&search) setTimeout(()=>search.focus(),20); };
  window.toggleRemakeMultiValueV6245=function(kind,value){ pushRemakeUndoV6266(); const text=String(value); const next=effectiveSelectionSetV6250(kind); if(next.has(text)) next.delete(text); else next.add(text); commitEffectiveSelectionV6389(kind,Array.from(next)); persistUiV6230(); renderDashboardV6230(); };
  window.onlyRemakeMultiValueV6245=function(kind,value,event){ if(event){event.preventDefault();event.stopPropagation();} pushRemakeUndoV6266(); setFilterValuesV6245(kind,[String(value)]); persistUiV6230(); renderDashboardV6230(); };
